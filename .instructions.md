# Repository AI Instructions

**Source of truth:** [`AGENTS.md`](AGENTS.md) and [`.agents/skills/`](.agents/skills/).

## Always-on reminders

- Run `./scripts/quality/quality_check.sh` or `./scripts/quality/quality_check.ps1`
  before tests.
- No lint suppressions, disables, or ignore-based bypasses.
- Coverage ≥ 90%; update `assets/coverage.svg` locally when regenerating coverage.
- Prefer skills: `code-linter`, `test-runner`, `pipeline-runner`, `ci-maintainer`,
  `release-hygiene`, `resolve-pr-comments`, `review-with-coderabbit`.

Keep this file thin. Change project law in `AGENTS.md`, not here.
